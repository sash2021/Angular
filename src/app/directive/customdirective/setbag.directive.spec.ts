import { SetbagDirective } from './setbag.directive';

describe('SetbagDirective', () => {
  it('should create an instance', () => {
    const directive = new SetbagDirective();
    expect(directive).toBeTruthy();
  });
});
